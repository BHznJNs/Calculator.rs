# class definition
Person = cls {:
    age;:
    name;:
    greet = fn (self $obj) {:
        out self.name;:
    };:
}

# instantiate class
inst = new Person[10, "test"]
# use brackets!<-^__________^

inst.greet() # 'test'